/*
 * 运行数据： ms  KB
 */
#include <bits/stdc++.h>

using namespace std;
int main(int argc, char const *argv[])
{
    int num, total;
    cin >> num >> total;
    int *store = new int[num];
    int *price = new int[num];
    for (int i = 0; i < num; i++) {
        cin >> store[i];
        cin >> skipws;
    }
    for (int i = 0; i < num; i++) {
        cin >> price[i];
        cin >> skipws;
    }
    return 0;
}