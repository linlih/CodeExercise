/*
   问题描述：
   解题思路： 
 */
#include <bits/stdc++.h>

using namespace std;

int num[10010];

int main(int argc, char const *argv[]) {
    #ifdef DEBUG
    freopen("input.txt","r",stdin);
    #endif
    int k;
    cin >> k;
    for (int i = 0; i < k; ++i) {
        cin >> num[i];
    }
    int max_sum = INT_MIN;
    int sum = 0;
    
    return 0;
}