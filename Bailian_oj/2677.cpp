#include <iostream>

using namespace std;
int main(int argc, char const *argv[])
{
    int n;
    int i, j;
    cin >> n;
    int g;
    int area = 0;
    int tmp_i, tmp_j;
    int **arr; // 数组指针
    arr = (int**)malloc(sizeof(int*)*n);  
    for(i = 0;i < r;i++)  
        arr[i] = (int*)malloc(sizeof(int)*n);
    for (i = 0; i < n; ++i) {
        for (j = 0; j < n; ++j) {
            cin >> g;
            a[i][j] = 
            if (g <= 50) {
                area ++;
                tmp_i = i;
                tmp_j = j;
            }
    
            
        }
    } 
    return 0;
}